precision mediump float;
varying vec2 fragmentTextureCoordinate;
uniform sampler2D baseColorTexture;
void main()
{
  gl_FragColor = texture2D(baseColorTexture,fragmentTextureCoordinate);
  //gl_FragColor = vec4(1.0,1.0,1.0,1.0);
}
